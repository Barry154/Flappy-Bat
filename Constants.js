import { Dimensions } from "react-native";

// Constant variables used by multiple class components
export default Constants = {
    MAX_WIDTH: Dimensions.get("screen").width,
    MAX_HEIGHT: Dimensions.get("screen").height,
    GAP_SIZE: 240,
    PIPE_WIDTH: 100,
    BAT_WIDTH: 50,
    BAT_HEIGHT: 50
}